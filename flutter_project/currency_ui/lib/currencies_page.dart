import 'package:flutter/material.dart';

class CurrenciesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Currencies")),
      body: Center(child: Text("Page for currencies")),
    );
  }
}
